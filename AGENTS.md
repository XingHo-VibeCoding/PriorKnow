# AGENTS.md — 本项目的协作规则

> 这个文件是给 AI 助手看的「项目说明书」。每次让 AI 帮忙之前，它应该先读一遍这里。
> 规则由我（项目主人）维护，AI 不得擅自删除或改写本文件的约定。

---

## 一、项目是什么

- **项目名**：PriorKnow（中文名「知先」）
- **一句话介绍**：以动态优先队列为核心的「下一个最优行动调度器」—— 输入一堆任务，它算出你现在最该做哪件，并解释为什么（面向传智杯这类比赛，形态为 H5 / PWA）。
- **当前阶段**：第 1 周 Day 6 —— 三份规范文档（research.md / PRD.md / TECH_DESIGN.md）已入库，**尚未开始写代码**（Day 7 开始）。技术路线已定：React + Vite + TypeScript，数据层做抽象，第 1–2 周挂 IndexedDB、第 3 周挂 CloudBase。

## 二、我是谁 / 我的基础

- **编程经验**：非完全零基础 —— 有 Gitee + 微信小程序（前后端合一仓库）经验，也改过仓库名；但本项目用到的 React / TypeScript / 优先队列基本从零开始。基础概念可跳，重点讲「为什么这么选、取舍是什么」。
- **交流方式**：请用大白话解释，先说「要做什么、为什么这么做」，再给具体操作。
- **术语要求**：出现英文或专业名词时，顺手用一句中文解释它是什么。

## 三、AI 协作规则（重要）

1. **先解释，再动手**：每次改动前，用一到两句话说明思路和影响范围。
2. **一次只做一小步**：做完一步停下来让我确认，不要一口气改一大堆。
3. **不确定就问**：需求模糊时先提问确认，不要自己猜着往下写。
4. **改动要透明**：改完之后明确列出「动了哪些文件、动了什么」。
5. **报错先说人话**：遇到报错，先用中文解释原因，再给修复方案。
6. **不做计划外的事**：我没有要求的依赖、脚本、文件夹，不要自作主张添加。
7. **命令要带说明**：执行命令时，告诉我这条命令是干什么的，让我也能看懂。
8. **不碰敏感信息**：任何密钥、密码、token 都不能写进代码或提交到仓库。

## 四、技术约定

| 项目 | 约定 |
| --- | --- |
| 运行环境 | Node.js（已装 v22.22.2）+ npm（v11.19.1） |
| 版本管理 | Git（已装 v2.55.0），**Day 2 之后**才开始用 |
| 主要工具 | WorkBuddy |
| 注释语言 | 中文 |
| 命名风格 | 文件夹与文件用「小写英文 + 短横线」，例如 `game-logic.js` |
| 代码存放 | 统一放在工作区文件夹的子目录内，不在根目录堆文件 |

## 五、目录约定

> 以下是仓库根目录的现有结构。等第 2 周真正开始写代码后，再补充 `core/`（算法）、`data/`（数据层）、`ui/`（界面）等目录。

```
PriorKnow/
├── AGENTS.md          # 本文件：项目协作规则
├── 项目描述.md        # 产品总纲（知先 / PriorKnow）
├── research.md        # 产品调研（Day 3，竞品对比）
├── PRD.md             # 产品需求文档（Day 4，MVP 功能与验收标准）
├── TECH_DESIGN.md     # 技术设计（Day 5，技术路线 / 数据模型 / 接口契约 / 数据流）
├── index.html         # 占位页（Day 2）
├── docs/              # 文档配图（如 data-flow.png 数据流图）
├── screenshots/       # 每日打卡截图
├── .gitignore         # 忽略规则
├── .gitattributes     # 换行符统一（LF）
└── 28天VibeCoding学习打卡计划.md   # 课程计划（本地保留，已加入忽略名单，不入库）
```

## 六、禁止事项

- 不在未备份的情况下删除或覆盖已有文件。
- 不一次性大改多个文件，不搞「顺手重构」。
- 不提交密钥、密码、token 等敏感信息。
- 不跳过解释直接给一大段看不懂的代码。

## 七、进度记录

- [x] **Day 1**：装齐环境（WorkBuddy / Git / Node.js）+ 注册 GitHub + 建好工作区 + 存入 AGENTS.md
- [x] **Day 2**：建立代码仓库（PriorKnow，Public）+ index.html 占位页 + 首次提交（含 GitHub 仓库首页截图）
- [x] **Day 3**：产品调研 —— research.md 对比 Todoist / 滴答清单 / 微软 To Do，结论：三家都在「记录」层卷，没人回答「我现在最该做哪件、为什么」；同时把课程计划文档移出版本管理
- [x] **Day 4**：写 PRD —— PRD.md（MVP 9 项功能、4 个视图、7 条异常与边界、17 条验收标准、AI 自检记录）；明确「砍掉云同步、暂缓优先级公式精细调参」
- [x] **Day 5**：技术设计 —— TECH_DESIGN.md（技术路线 / 三层分工 / 数据层抽象 / 数据模型 / 接口契约 / 数据流图）+ docs/data-flow.png
- [x] **Day 6**：检查并完善 AGENTS.md —— 核查时发现三条 Day 1 约束缺失，补回第八节；新增第九节个人规则两条（不代笔、改动附验证方式）
- [ ] **Day 7**：待补充

---

## 八、任务边界与推进方式（Day 6 补回）

> 本节是 Day 6 核查时发现缺失、按 Day 1 原始模板补回的。上面已有的内容一字未动。

**任务边界**

1. 我发来的「今日任务清单」就是**今天唯一的任务范围**，只做清单里写的事。
2. **不提前做以后的天数。** 只有我说「**现在开始做 Day X**」，才允许动那天。
3. 不做清单以外的工作：不建多余的文件、不写清单没提的功能、不顺手优化已有代码。你觉得可以改得更好，先说一句，等我点头。
4. 拿不准是否在范围内，先问我，不要猜。

**推进方式**

1. 每做完一步就停下，等我回复「**进入下一板块**」再往下做。我没说这四个字，你就不要继续。
2. 「进入下一板块」只对当前那一步有效，**不代表可以一路做到收尾**。
3. 每步做完告诉我三件事：做了什么 / 改了哪些文件 / 我怎么亲手验证。

**需要我拍板的地方**

1. 选方案、起名字、定风格、改需求范围这类决定，**把选项和各自的代价列给我选，不要自己决定**。
2. 我拍板之前不要先动手，也不要造成「已经是这样了」的既成事实。

## 九、我的个人规则（Day 6 追加）

### 1. 留给我自己写的内容，不要代笔

- 文档里凡是标明「留给我自己写」「本节由我撰写」的章节，**只给提纲、要点和素材，不要替我写成段落**。
- 我写完初稿后，你可以帮我理顺句子、指出矛盾和遗漏，但要保留我的原意和判断。
- **防的是**：我会把该自己想清楚的部分外包出去。research.md 的第 6 节、PRD.md 的第 9 节都是我亲手写的，也是我真正想明白问题的地方——你顺手写完，我就失去了这次练习的机会。我要的是「想到」，不是「拿到」。

### 2. 每次改动都要告诉我怎么亲手验证

- 每次改完，除了「改了哪些文件」，还要说清「**我怎么亲手确认它成了**」：打开哪个文件、点哪里、该看到什么、不该看到什么。
- 优先给我能亲眼看到的结果（页面能打开、文件里能看到那段文字、命令有输出），不接受「已完成」「已修复」这种口头结论。
- **防的是**：我只能靠你的结论判断对错。看不见验证方式，我就只能盲信——那样既学不到东西，也发现不了问题。

---

_最后更新：2026-09-21_
