# 忽略文件（.gitignore）
# 作用：告诉 Git 哪些文件「不要」存进仓库。写在这里的文件不会被提交，也不会被上传到 GitHub。

# ---- 敏感信息：绝不允许上传 ----
.env
.env.*
!.env.example
*.pem
*.key
*.p12
secrets.json

# ---- 依赖目录 ----
node_modules/
.pnpm-store/
vendor/
# npm 的本地缓存。WSL 里 ~/.npm 不可写，装依赖时必须用 --cache 指到仓库内，
# 不忽略的话 `git add -A` 会把几十兆缓存一起提交（Day 8 收尾时差点中招）。
.npm-cache/

# ---- 构建产物 ----
dist/
build/
out/
.next/
*.tsbuildinfo

# ---- 本地数据库文件 ----
*.sqlite
*.sqlite3
*.db
*.db-journal
*.db-wal

# ---- 日志 ----
*.log
logs/

# ---- 仅本地保留的课程/个人资料（不上传仓库） ----
28天VibeCoding学习打卡计划.md
day7-week1-validation.md
day8-dsh-handoff.md

# ---- 工具与编辑器临时文件 ----
.workbuddy/
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~
