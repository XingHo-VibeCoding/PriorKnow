# PRD.md — 知先 · PriorKnow

> **产品需求文档（Product Requirements Document）**
> 别问先做什么，知先告诉你现在做什么。｜ *Know what to do now.*
>
> 版本：v1.0 · 2026-09-19 ｜ 依据：`research.md`（竞品调研）+ `项目描述.md`（产品总纲）
> 阶段：第 1 周 Day 4 ｜ **本文档不涉及技术选型与代码实现**

---

## 一、产品概述

**知先（PriorKnow）是一款以动态优先队列为核心的「下一个最优行动调度器」。**

用户把自己的任务丢进来，知先不满足于「记下来」，而是回答那个更难的问题：
**我现在最该做哪一件？为什么是它？**

产品由三件事撑起来：

1. **自动算优先级** —— 用户不手动标 P1/P2，系统按任务自身的属性算分排序；
2. **队列看得见** —— 任务顺序在界面上呈现为一条队列，参数一变，队列当场重排（有动画）；
3. **理由说得出** —— 每个排在前面的任务都能看到「为什么是它」，不是黑盒。

> `research.md` 的结论：Todoist 靠用户手动标优先级、滴答清单的四象限要手动归类、Microsoft To Do 的建议不解释理由。
> **三款都停在「记录」，没有一款做「可解释的自动调度」——这就是知先站的位置。**

---

## 二、目标用户

| 用户 | 场景 | 现有工具的痛点 |
| --- | --- | --- |
| **期末周学生**（首要） | 多门作业、实验报告、考试复习同时压着 | 清单很长，但不知道该先复习哪科 |
| 备考人群 | 长期目标已拆解成日常任务 | 每天对着任务列表发呆，先挑简单的做 |
| 多任务工作者 | 需求、会议、文档、沟通交织 | 一天被切碎，重要的事一拖再拖 |

**共性需求**：不是「没有工具记录」，而是「工具不能替我决定」。

**使用场景举例（首要用户的真实一天）**：

> 周五晚上，手上压着 8 件事：下周一交的实验报告（明天得开工，2 小时，很重要）
> 下周的英语作文、还有几件杂事。他知道报告要紧，却总忍不住先去做那些十分钟就能
> 划掉的杂事。他打开知先，输入「今天还剩 3 小时」，得到一条只装得下的队列——
> 报告排在第一，理由写着「明天截止 + 重要度 5 星 + 需要 2 小时 → 今天不做就来不及」。

---

## 三、MVP 范围（本期做）

> 范围依据 Day 3 的决定：**自动优先级计算 + 优先队列排序算法 + 前端界面**。
> 每一项都必须在 28 天内可拆成小任务、可独立验收。

| 编号 | 功能 | 一句话说明 |
| --- | --- | --- |
| F1 | 任务增删改查 | 能加、能改、能删、能标记完成 |
| F2 | 自动优先级计算 | 系统算分，用户不手动标优先级 |
| F3 | 优先队列可视化 | 任务顺序以队列形态呈现，看得见排序 |
| F4 | 队列重排动画 | 参数变化时，队列当场重排，上浮/下沉有动画 |
| F5 | 可解释排序 | 每个任务显示「为什么排这个位置」 |
| F6 | 今日时间预算 | 输入今天剩几小时，自动筛出今天做得完的一组任务 |
| F7 | 专注模式 | 只显示队首任务 + 倒计时，做完下一个自动浮上来 |
| F8 | 本地保存与导入导出 | 数据存本地、离线可用、可导出 JSON 备份 |
| F9 | 示例数据 | 内置「期末周冲刺」示例，30 秒看懂产品价值 |

### 3.1 功能明细（用户动作 + 可观察结果）

#### F1 任务增删改查

- **用户动作**：在首页点「加任务」，填写标题、截止时间、预计耗时、重要度，保存。
- **可观察结果**：新任务出现在队列中（位置由系统算定）；用右键或卡片菜单可编辑、删除；勾选后可标记完成，卡片离开队列。
- **用户动作**：勾选一个任务完成。
- **可观察结果**：该任务从待办队列消失，进入「已完成」；队列里其余任务的顺序**当场重排**。

#### F2 自动优先级计算

- **用户动作**：填入「截止时间 + 重要度 + 预计耗时」后保存，**不做任何优先级标记**。
- **可观察结果**：任务卡片上出现一个系统给出的优先级分数；用户从未选择过 P1/P2，排序却已经发生。
- **产品规则（本 PRD 只说规则，不含实现参数）**：优先级由 **截止时间的紧迫程度、重要度、预计耗时** 三者共同决定——越紧迫、越重要、越省时，排得越前；具体权重与公式留给 Day 5 技术设计。

#### F3 优先队列可视化

- **用户动作**：打开首页。
- **可观察结果**：任务不是平铺的列表，而是一条**有明确先后次序的队列**；排第一的任务在最显眼位置。
- **用户动作**：滚动/浏览队列。
- **可观察结果**：每个位置都能看出前后关系（第 1、第 2、第 3……不出现并列含糊）。

#### F4 队列重排动画

- **用户动作**：把某个任务的截止时间从「下周」改成「明天」，保存。
- **可观察结果**：该任务卡片**移动的过程被看见**——从原位置浮到新位置，其他卡片同步让位，全程在 **1 秒内**完成，不出现界面闪烁或跳变。

#### F5 可解释排序

- **用户动作**：点开排第一的任务卡片。
- **可观察结果**：出现一行人话解释，如「**明天截止 + 重要度 5 星 + 只需 1 小时 → 排第 1，建议现在做**」；解释中列出的是**具体是哪几项把它抬上去的**，不是一句「系统推荐」。

#### F6 今日时间预算

- **用户动作**：在首页输入「今天还剩 3 小时」。
- **可观察结果**：队列被筛成一组**总耗时不超过 3 小时**的任务，并按优先级排好；界面上能看出「今天这几件做得完」。
- **用户动作**：把预算从 3 小时改成 30 分钟。
- **可观察结果**：今天的队列立刻变短，只留短平快的任务，超预算的被移出今日视图（但仍留在全量队列里，不被删除）。

#### F7 专注模式

- **用户动作**：在队首任务上点「开始专注」。
- **可观察结果**：进入只剩一张卡片的界面 + 一个倒计时；倒计时结束有明确提示。
- **用户动作**：在专注模式里点「完成」。
- **可观察结果**：当前任务消失，**下一个任务自动浮上来**，无需返回列表手动选择。

#### F8 本地保存与导入导出

- **用户动作**：添加任务后关闭浏览器，重新打开。
- **可观察结果**：任务仍在，顺序不变——数据没有丢。
- **用户动作**：断网后打开并使用。
- **可观察结果**：功能照常，不出现「网络错误」。
- **用户动作**：点「导出数据」。
- **可观察结果**：下载到一个 JSON 文件；用「导入数据」选它，能还原任务列表。

#### F9 示例数据

- **用户动作**：第一次打开（或点「载入示例」）。
- **可观察结果**：出现一组「期末周冲刺」任务，队列已排好、解释已生成，**新用户在 30 秒内能说出这个产品在干什么**。

---

## 四、后续功能（本期不做）

> 依据 `research.md` 第 5 节。**列在这里的功能，本期一行代码都不写。**

| 功能 | 本期不做的原因 |
| --- | --- |
| 账号系统 / 云同步 / 多端同步 | 三个竞品全以云为核心，本地优先才是知先的差异；云同步会拖垮 28 天进度 |
| 日历视图 / 日历订阅 | 滴答清单的强项，正面卷没有胜算，且与「调度」主线弱相关 |
| 番茄钟 / 习惯打卡 | 滴答清单已做得很重，属功能堆叠，会稀释核心价值 |
| 社交 / 协作共享清单 | 定位是「个人决策」，不是团队协作 |
| 游戏化积分（模仿 Todoist Karma） | 已验证有效，但与「可解释调度」主线无关 |
| 消息推送 / 广告 / 任何付费服务 | 个人项目无商业化诉求 |
| 复杂依赖关系（前置任务锁定） | 会让排序逻辑复杂度翻倍，首版先做单任务维度 |
| AI 增强解析（云端大模型） | 涉及密钥与按量计费风险；首版用本地规则解析够用 |
| 长期任务的阶段推进（阶段目标 / 里程碑） | **本期不做**。起因是「长任务在 7 天紧迫度窗口外会被小事长期压住」（2026-09-22 记录）；改动要动数据模型与算分规则，留待第 2 周验收后再评估，见下方说明 |

**已明确边界的一条**：任务的自然语言快速添加（如「明天下午交实验报告，2 小时，很重要」）**首版做**，但只覆盖最常用句式（时间 + 时长 + 重要度），用本地规则解析，**不接任何外部 AI 服务**。

**已记录待评估的一条（2026-09-22）**：长期任务的阶段推进。起因是「28 天打卡」与「每天背单词」这两类需求——**它们看似同类，其实归属不同**：

- **「每天背单词」**是无限重复任务，数据模型没有循环字段 → 归本节的「番茄钟 / 习惯打卡」，本期不做。
- **「28 天打卡」**是有终点的长期任务。知先的设计前提就是「长期目标已拆解成日常任务」（见第二节），所以**今天的数据模型已支持它**（拆成 28 个日任务或 4 个周任务即可），不需要新功能。

真正待评估的是**一个已被暴露的算法特性**，而非功能缺失：紧迫度窗口为 7 天，因此 28 天后到期的任务在前 21 天紧迫度恒为 0，会被任何「7 天内到期」的小事压住，直到最后一周才浮上来。这符合已拍板的公式，**不算缺陷**，但会让长任务的推进节奏断裂。

**若后续要处理，最小改法是给任务加「阶段目标」让长任务被看见，而不是上完整的习惯打卡系统**（后者已在本节明确排除）。评估时机：待第 2 周前端验收（Day 14）后，与其他候选功能一并排序决定。

---

## 五、页面与交互要求

本期规划 **3 个核心视图 + 1 个入口**。

### 5.1 视图一：今日队列（首页）

- **谁**：任何用户，打开即见。
- **做什么**：看队列、加任务、改任务、设今日时间预算。
- **看到什么**：顶部是「今日预算」输入框（如「还剩 3 小时」）；下方是排好序的任务卡片队列；排第一的卡片视觉上明显突出；每张卡片有优先级分数；点卡片展开「为什么排这里」的解释。
- **交互要求**：任何修改（改截止、改重要度、勾完成）后，队列 **1 秒内**完成重排并伴随动画。

### 5.2 视图二：任务详情 / 编辑

- **谁**：想改某个任务的用户。
- **做什么**：改标题、截止时间、预计耗时、重要度；或删除任务。
- **看到什么**：一个表单，字段与数据字段一一对应；**重要度和耗时用可拖动的滑块**，拖动时队列的变化能实时预览（这是「算法可玩」的入口）。
- **交互要求**：保存后回到首页，位置变化要能看见；放弃修改可取消，不产生脏数据。

### 5.3 视图三：专注模式

- **谁**：已经决定开干的用户。
- **做什么**：只看当前这一件，倒计时结束或提前完成后进入下一件。
- **看到什么**：屏幕中央**只有一张**任务卡片 + 倒计时；完成按钮和放弃按钮清晰可辨。
- **交互要求**：完成后下一张卡片自动浮出（不跳回列表）；退出专注模式回到首页时，队列顺序与进入前一致（除非中间有完成操作）。

### 5.4 入口：数据与设置

- **做什么**：导出 JSON、导入 JSON、载入示例数据。
- **看到什么**：三个明确的操作按钮。
- **交互要求**：导入非法文件时给中文提示，不覆盖现有数据。

---

## 六、数据字段

每个任务包含以下字段：

| 字段 | 说明 | 是否必填 |
| --- | --- | --- |
| `id` | 任务唯一标识，系统生成 | 系统 |
| 标题 | 任务内容，一句话 | ✅ 必填 |
| 截止时间 | 什么时候必须交付 | 可空（无截止的任务排在后面） |
| 预计耗时 | 预计要花多久（分钟） | 可空（默认给一个保守估计） |
| 重要度 | 1–5 星 | ✅ 必填，默认 3 星 |
| 当前状态 | 待办 / 进行中 / 已完成 | 系统 |
| 创建时间 | 用于排序时打破同分僵局 | 系统 |
| 优先级分数 | **系统算出的结果，用户不可直接编辑** | 系统派生 |

> 说明：首版**不做**「依赖任务」字段（见第四节）。

---

## 七、异常与边界状态

必须显式处理，不能留白屏或报错给用户看。

| 情况 | 界面表现 |
| --- | --- |
| 一个任务都没有 | 显示引导文案 + 「载入示例数据」按钮（不显示空队列） |
| 任务没有截止时间 | 不参与紧迫度计算，排在有时限的任务之后，卡片上标注「无期限」 |
| 任务已逾期（截止时间已过） | 明确标红提示「已逾期」，并排到队列最前 |
| 今日预算小于最短任务耗时 | 提示「今天的时间不够完成任何一项」，并给出两条明确出路：① 修改预算；② 查看全部任务（可临时下调某项任务的耗时估算）。不显示空列表 |
| 导入的文件格式不对 | 中文提示「文件格式不正确，已保留你原来的数据」，原数据不动 |
| 浏览器的本地存储写入失败 | 明确提示「保存失败，请先导出备份」，不静默丢数据 |
| 断网 | 一切照常（本地优先），不出现网络相关报错 |

---

## 八、验收标准

> 逐条可打勾。全部勾上才算 MVP 核心闭环完成。

**基础闭环**

- [ ] 能新增任务，填写标题 / 截止时间 / 预计耗时 / 重要度
- [ ] 能编辑、删除、标记完成任务
- [ ] 关闭浏览器后重新打开，任务与顺序都还在

**核心差异（重点）**

- [ ] 用户从未手动标优先级，任务卡上就有系统算出的优先级分数
- [ ] 队列中任意相邻两项，前一项的优先级分数不低于后一项（顺序与分数不矛盾，算法算错能被看出）
- [ ] 改一个任务的重要度或截止时间，队列**当场重排**且能看到移动过程
- [ ] 排在前面的任务能点开看到「为什么是它」的人话解释，解释中列出具体是哪几项造成的
- [ ] 「为什么」的解释与队列顺序**一致**（不能出现解释说第一、界面排第三）

**今日与执行**

- [ ] 输入「今天剩 3 小时」，能筛出总耗时不超预算的一组任务
- [ ] 预算改小后，今日视图变短，且被移出的任务没有从全量数据里消失
- [ ] 专注模式只显示队首一件，完成后下一件自动浮上来

**数据与健壮**

- [ ] 断网状态下可正常使用
- [ ] 导出 JSON 后能成功导入还原
- [ ] 导入非法文件时给出中文提示且原数据不丢
- [ ] 空状态、逾期任务、预算不足三种情况都有明确界面表现，不白屏

**可讲清**

- [ ] 随机挑一个功能，能用「谁、在哪个页面、做什么、看到什么」四句讲完
- [ ] 内置示例数据能让没听过这个产品的人在 30 秒内说出它解决什么问题

---

## 九、我砍掉了什么功能，为什么

**1. 云端数据同步 —— 直接砍掉。**

首版时间有限，云服务对接复杂，而且它不属于 MVP 的核心。首版的时间更应该花在打磨 MVP 上。云同步会在 Day 15 之后作为目标推进。

**2. 优先级公式的精细调参 —— 缩水，不是砍掉。**

排序算法本身是知先的核心，必须做；但「把权重调到最优」这件事往后放。首版最重要的是先跑起来，所以先用一版朴素权重，等整体稳定下来，再回头调整优化排序函数。

---

## 附：AI 自检记录（2026-09-19）

**自检依据**：课程 Day 4 的「完成后检查」三项 + 「完成标准」三项。

### 检查项逐条过

| 检查项 | 结果 |
| --- | --- |
| 随机挑一个功能，能回答「谁、在什么页面、做什么、看到什么」 | ✅ 抽查 F6 今日时间预算：**期末周学生** / 在**首页（今日队列）顶部预算框** / **输入今天剩余小时数** / 看到**队列被筛成不超预算的一组并排好序** |
| PRD 有 MVP 与后续的分界 | ✅ 第三节（F1–F9）/ 第四节（9 项明确不做，各附原因） |
| 至少三个页面或视图的初步规划 | ✅ 第五节省 4 个（今日队列 / 任务编辑 / 专注模式 / 数据设置） |
| PRD.md 含验收标准 | ✅ 第八节，17 条可打勾 |
| 每个 MVP 功能都有「用户动作 + 可观察结果」 | ✅ F1–F9 全覆盖 |
| 无「以后再说但没有边界」的空泛表述 | ✅ 第四节每项都写了不做的原因；自然语言解析明确圈定「只做时间+时长+重要度三种句式，不接外部 AI」 |

### 自检中发现并当场修正的 4 处

1. **（必须修改）** 第八节有笔误「Day 1 周的核心闭环」→ 已改为「MVP 核心闭环」。
2. **（必须修改）** 验收标准缺一条算法正确性判据——只验「能重排」而不验「排得对」，算错了也过得了验收 → 已补「队列中任意相邻两项，前一项分数不低于后一项」。
3. **（建议修改）** F4 原写「全程不超过**约** 1 秒」，「约」字导致不可判定 → 已改为「全程在 1 秒内完成」。
4. **（建议修改）** 异常状态「预算小于最短任务耗时」原本建议用户「看看 30 分钟能做的小事」，逻辑自相矛盾（30 分钟的任务可能同样超预算）→ 已改为给出「改预算」和「查看全部任务并下调耗时估算」两条明确出路。

### 结论

修正后重过一遍检查项，**无「必须修改」级问题残留**。剩余为待办项而非缺陷：

- 优先级公式的具体权重按约定不在本 PRD 展开，留待 Day 5 技术设计。

### 验收前复核（2026-09-20）

第九节由项目主人补写后复核，发现并修正 1 处**自相矛盾**：

- 原文写「砍掉了**优先级排序算法**」，与该算法在第三节（F2/F3，MVP 核心）、第八节（验收标准两条专验）的定位冲突，也与 research.md 确立的差异化矛盾。经与项目主人确认，其本意为「砍掉**公式的精细调参**」而非算法本身，已按此修正表述。

---

_本文档内容由 AI 依据 `research.md` 生成 ｜ 决策与取舍：项目主人 ｜ 2026-09-20_
